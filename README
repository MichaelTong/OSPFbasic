====How to Use===
Enter the source file directory, execute 'make', it will compile and generate executable file THOSPF.

Edit conf file according to the example file under 'conf' directory, so that you can configure OSPF.

You still have to configure IP in system setup.